// import * as types from './mutation-types'

export const viewCameraDetails = ({ commit }, camera) => {
  if (camera.id !== null) {
    /* commit(types.ADD_TO_CART, {
      id: product.id
    }) */
    this.$router.push()
  }
}
